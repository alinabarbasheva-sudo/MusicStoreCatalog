<UserControl x:Class="MusicStoreCatalog.Pages.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="550" Width="950">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="👥 Управление консультантами" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <!-- Панель управления -->
        <Border Grid.Row="1" 
                Background="#f8f9fa" 
                Padding="10" 
                CornerRadius="5"
                Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddUserBtn" 
                        Content="➕ Добавить консультанта" 
                        Padding="10,6"
                        FontWeight="SemiBold"
                        Background="#3498db"
                        Foreground="White"></Button>
            </StackPanel>
        </Border>

        <!-- Таблица консультантов -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="UsersGrid" 
                      AutoGenerateColumns="False" 
                      SelectionMode="Single"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      RowBackground="White"
                      AlternatingRowBackground="#f8f9fa"
                      Width="920">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="120"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="100"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="120"/>
                    <DataGridTextColumn Header="Специализация" Binding="{Binding Specialization}" Width="130"/>
                    <DataGridTextColumn Header="Продажи" Binding="{Binding SalesCount}" Width="80">
                        <DataGridTextColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#2c3e50"/>
                            </Style>
                        </DataGridTextColumn.CellStyle>
                    </DataGridTextColumn>

                    <!-- Действия -->
                    <DataGridTemplateColumn Header="Действия" Width="100" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑️ Удалить" 
                                        Padding="8,4"
                                        Tag="{Binding ID}" 
                                    Click="DeleteUserBtn_Click"
                                    Background="#e74c3c"
                                        Foreground="White"
                                        FontWeight="SemiBold"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Редактировать" Width="110" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="✏️ Редактировать" 
                    Padding="8,4"
                    Tag="{Binding ID}" 
                    Click="EditUserBtn_Click"
                    Background="#3498db"
                    Foreground="White"
                    FontWeight="SemiBold"
                    ToolTip="Редактировать данные консультанта"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>